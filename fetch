#!/usr/bin/env bash
#
# Fetch the latest changes for all bare git repos in ref/ from Gerrit.
#
set -euo pipefail
. "$(dirname "$0")"/lib

# Find all bare git repos (directories ending in .git) and fetch each one
find ref -name '*.git' -type d | while read -r repo; do
  git -C "$repo" fetch
done
