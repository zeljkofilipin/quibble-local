#!/usr/bin/env bash
#
# Run shellcheck on all shell scripts in the repo.
#
set -euo pipefail

shellcheck clean deep_clean deep_test dependencies fetch fresh_install help install lib lint prepare run_selenium_tests shell test
